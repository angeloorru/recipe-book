{
  "name": "recipe-book",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "postinstall": "(cd api && npm install) && (cd ui && npm install) && (cd e2e && npm install)",
    "lint-be": "cd api && npm run lint",
    "lint-fe": "cd ui && npm run lint",
    "test-fe": "cd ui && npm test"
  }
}
